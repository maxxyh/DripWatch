# DripWatch PWA

Mobile-first Next.js companion for the native DripWatch app. It reads and writes the same
Supabase notebook through guarded server routes, preserves the Swift wire contract, and provides
a read-only offline snapshot.

Setup, deployment, security boundaries, and verification are documented in
[`../docs/web-pwa.md`](../docs/web-pwa.md).
